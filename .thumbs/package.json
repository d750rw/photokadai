{
  "name": "thumbs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compress": "mkdir -p ../public/lg/thumbs && mkdir -p ../public/lg/images && mkdir -p ../album-images-processed && node index.js true"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "image-size": "^1.0.1",
    "image-thumbnail": "^1.0.14"
  }
}
